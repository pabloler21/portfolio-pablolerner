---
interface Props {
  lang?: 'en' | 'es';
  surface?: 'game' | 'doc';
  /* La ruta hermana ya resuelta por Base (que es quien sabe que paginas
     existen). Antes estos links eran fijos a /en/ y /es/, asi que desde
     /en/risk/ el toggle te dejaba en la home española, no en /es/risk/. */
  altHref?: string;
}

const { lang = 'en', surface = 'doc', altHref = '/en/' } = Astro.props;
const aqui = Astro.url.pathname;

const now = new Date();
const sysDate = `${now.getFullYear()}.${String(now.getMonth() + 1).padStart(2, '0')}.${String(now.getDate()).padStart(2, '0')}`;

---

<footer class="status-bar">
  {/* Los controles de juego sólo donde hay juego. */}
  {surface === 'game' ? (
    <div class="controls">
      <span class="control"><span class="key">◆</span>SELECT</span>
      <span class="control"><span class="key">↵</span>CONFIRM</span>
      <span class="control"><span class="key">ESC</span>BACK</span>
    </div>
  ) : <span />}

  <div class="bar-center">
    {/* El CV vive en la banda de identidad: acá estaba duplicado. */}
    <div class="lang-toggle">
      <a href={lang === 'en' ? aqui : altHref} class:list={['lang-btn', { active: lang === 'en' }]} aria-label="English">EN</a>
      <span class="sys-text">|</span>
      <a href={lang === 'es' ? aqui : altHref} class:list={['lang-btn', { active: lang === 'es' }]} aria-label="Español">ES</a>
    </div>
  </div>

  {/* LAT/LON/NODE es telemetría inventada: es atrezzo del juego, no
      información, y no tiene lugar en un documento. */}
  {surface === 'game'
    ? <span class="coord sys-text">LAT:-34.6037 LON:-58.3816 · {sysDate} · NODE:ACTIVE</span>
    : <span class="coord sys-text">{sysDate}</span>}
</footer>


<style>
  .status-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.45rem 1rem;
    border-top: var(--border);
    background: color-mix(in srgb, var(--bg-void) 90%, black);
    gap: 1rem;
    flex-wrap: wrap;
  }

  .controls {
    display: flex;
    gap: 1.2rem;
  }

  .control {
    display: flex;
    align-items: center;
    gap: 0.3rem;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.15em;
    color: var(--sand-dim);
  }

  .key {
    border: var(--border);
    padding: 0.05rem 0.3rem;
    font-size: 0.7rem;
    color: var(--sand-dim);
  }

  .bar-center {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .lang-toggle {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .lang-btn {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.18em;
    color: var(--sand-dim);
    transition: color 0.15s;
  }

  .lang-btn:hover,
  .lang-btn.active {
    color: var(--accent-bright);
  }

  .coord {
    font-size: 0.7rem;
  }

  @media (max-width: 640px) {
    .coord { display: none; }
    .controls { gap: 0.8rem; }
  }

  @media (max-width: 480px) {
    .controls { display: none; }
  }
</style>
